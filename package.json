{
  "name": "@dnn/fake-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "json-server --watch db.json --port 3001"
  },
  "author": "Dênerson Pinas",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
